# Copy to .env for local docker-compose use. These are obviously-dev values —
# never use them anywhere reachable from the internet.
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=devpassword
JWT_SECRET=dev-only-secret-change-me-min-32-bytes-long
